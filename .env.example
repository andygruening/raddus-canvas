# Local server port. Defaults to 5174 when unset.
PORT=5174

# Optional path for the local JSON data store.
# Leave blank to use the platform default documented in README.md.
RADDUS_CANVAS_DATA_FILE=

# Do not commit Anthropic API keys or other secrets.
# Enter Anthropic API keys through the local app sign-in flow.
